server.port=${PORT:5000}    
# Commented out prod profile for local development
# spring.profiles.active=prod
spring.datasource.url=jdbc:postgresql://localhost:5432/SkillStream_springboot
spring.datasource.username=postgres
spring.datasource.password=Lakshit@123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.show-sql=true

# Allow Hibernate to continue even if database connection fails initially (for testing)
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

jwt.secret=${JWT_SECRET:change_this_secret_for_prod}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}